# Üye Giriş Paneli — Telefon + Şifre Auth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `uye-girisi.html`'e telefon+şifre tabanlı kimlik doğrulama ekle; herhangi biri başkasının üyelik bilgisine erişemesin.

**Architecture:** Frontend 4-state makine (phone → setPassword/login → card). Backend Google Apps Script, HMAC-SHA256 imzalı 30 günlük token. Şifreler SHA-256+salt ile hash'li Google Sheet'te saklanır. Şifresiz üye ilk girişte şifre belirler + KVKK onaylar.

**Tech Stack:** Vanilla HTML/CSS/JS, Google Apps Script (V8 runtime), Google Sheets

---

## Dosya Haritası

| Dosya | Değişiklik |
|-------|-----------|
| `apps-script/Code.gs` | YENİ — Apps Script referans kopyası |
| `uye-girisi.html` | GÜNCELLE — CSS ekle, HTML güncelle, JS yeniden yaz |
| Google Sheets | MANUEL — 3 yeni sütun ekle |
| Apps Script Editor | MANUEL — Code.gs kopyala + yeni sürüm deploy |

---

## Task 1: Google Sheet — 3 Yeni Sütun

**Files:**
- Modify: Google Sheets (manual)

- [ ] **Step 1: Sheet'i aç**

Google Sheets "Üyeler" sekmesini aç. Mevcut son veri sütununun SAĞINDAKİ 3 sütunun 1. satırını (başlık satırı) şöyle doldur:

| Sütun | Başlık |
|-------|--------|
| H (veya uygun boş) | `sifre_hash` |
| Bir sağı | `sifre_salt` |
| Bir daha sağı | `kvkk_tarih` |

> **Önemli:** Sütun numaraları farklıysa `Code.gs` üstündeki sabitleri güncelle:
> ```javascript
> const COL_HASH = 8;  // H = 8, I = 9, J = 10 ...
> const COL_SALT = 9;
> const COL_KVKK = 10;
> ```

- [ ] **Step 2: Mevcut sütun sırasını doğrula**

Sheet'teki sütun sırasını kontrol et ve Code.gs sabitleriyle eşleştir:

```
COL_AD        = A sütunu numarası
COL_TELEFON   = B sütunu numarası
COL_BRANS     = C sütunu numarası
COL_BASLANGIC = D sütunu numarası
COL_BITIS     = E sütunu numarası
COL_HAFTA     = F sütunu numarası
COL_ODEME     = G sütunu numarası
```

---

## Task 2: Apps Script — TOKEN_SECRET Ayarla

**Files:**
- Modify: Apps Script Project Settings (manual)

- [ ] **Step 1: Script Properties aç**

Apps Script editöründe sol menü → ⚙️ Project Settings → Script Properties → Add property

- [ ] **Step 2: TOKEN_SECRET ekle**

```
Property: TOKEN_SECRET
Value:    CimcimPark2026!SecretXk9mP#7qR   (en az 20 karakter, rastgele)
```

> TOKEN_SECRET değişirse tüm aktif token'lar geçersiz olur — üyeler tekrar giriş yapmak zorunda kalır.

---

## Task 3: Code.gs Yaz

**Files:**
- Create: `apps-script/Code.gs`

- [ ] **Step 1: `apps-script/Code.gs` dosyasını oluştur**

```javascript
// ═══════════════════════════════════════════════════════
// CİMCİMPARK Üye Portal — Apps Script Backend v2.0
// ═══════════════════════════════════════════════════════

// ── Sütun numaraları — Sheet yapına göre düzenle ───────
const COL_AD        = 1;   // A
const COL_TELEFON   = 2;   // B
const COL_BRANS     = 3;   // C
const COL_BASLANGIC = 4;   // D
const COL_BITIS     = 5;   // E
const COL_HAFTA     = 6;   // F
const COL_ODEME     = 7;   // G
const COL_HASH      = 8;   // H — sifre_hash
const COL_SALT      = 9;   // I — sifre_salt
const COL_KVKK      = 10;  // J — kvkk_tarih

const SHEET_NAME   = 'Üyeler';
const MAX_ATTEMPTS = 5;
const LOCK_TTL     = 900; // 15 dakika (saniye)

// ── Ana Router ─────────────────────────────────────────
function doGet(e) {
  try {
    const action = String(e.parameter.action || '').trim();
    if (action === 'checkStatus') return checkStatusHandler(e);
    if (action === 'setPassword') return setPasswordHandler(e);
    if (action === 'login')       return loginHandler(e);
    if (action === 'me')          return meHandler(e);
    return jsonResponse({ error: 'Geçersiz istek' });
  } catch (err) {
    Logger.log('doGet error: ' + err);
    return jsonResponse({ error: 'Sunucu hatası' });
  }
}

// ── checkStatus ────────────────────────────────────────
function checkStatusHandler(e) {
  const phone = normalizePhone(e.parameter.q || '');
  if (phone.length !== 10) return jsonResponse({ error: 'Geçersiz numara' });

  const row = findRow(phone);
  if (!row) return jsonResponse({ exists: false, hasPassword: false });

  const hash = String(row[COL_HASH - 1] || '').trim();
  return jsonResponse({ exists: true, hasPassword: hash !== '' });
}

// ── setPassword ────────────────────────────────────────
function setPasswordHandler(e) {
  const phone    = normalizePhone(e.parameter.q || '');
  const password = String(e.parameter.password  || '');
  const kvkk     = e.parameter.kvkk === 'true';

  if (phone.length !== 10) return jsonResponse({ success: false, error: 'Geçersiz numara' });
  if (password.length < 8) return jsonResponse({ success: false, error: 'Şifre en az 8 karakter olmalı' });
  if (!kvkk)               return jsonResponse({ success: false, error: 'KVKK onayı gerekli' });

  const sheet = getSheet();
  const data  = sheet.getDataRange().getValues();
  let rowIdx  = -1;

  for (let i = 1; i < data.length; i++) {
    if (normalizePhone(String(data[i][COL_TELEFON - 1])) === phone) { rowIdx = i; break; }
  }

  if (rowIdx === -1) return jsonResponse({ success: false, error: 'Bu numaraya ait kayıt bulunamadı' });

  const existingHash = String(data[rowIdx][COL_HASH - 1] || '').trim();
  if (existingHash !== '') {
    return jsonResponse({ success: false, error: 'Şifre zaten belirlenmiş. Değiştirmek için merkezi arayın.' });
  }

  const salt  = generateSalt();
  const hash  = hashPassword(salt, password);
  const today = Utilities.formatDate(new Date(), 'Europe/Istanbul', 'yyyy-MM-dd');

  const r = rowIdx + 1;
  sheet.getRange(r, COL_HASH).setValue(hash);
  sheet.getRange(r, COL_SALT).setValue(salt);
  sheet.getRange(r, COL_KVKK).setValue(today);

  return jsonResponse({ success: true, token: createToken(phone) });
}

// ── login ──────────────────────────────────────────────
function loginHandler(e) {
  const phone    = normalizePhone(e.parameter.q || '');
  const password = String(e.parameter.password  || '');

  if (phone.length !== 10) return jsonResponse({ success: false, error: 'Geçersiz numara' });

  const cache   = CacheService.getScriptCache();
  const lockKey = 'lock_' + phone;
  const attKey  = 'att_'  + phone;

  if (cache.get(lockKey)) {
    return jsonResponse({ success: false, error: 'Çok fazla yanlış deneme. 15 dakika sonra tekrar deneyin.' });
  }

  const row = findRow(phone);
  if (!row) {
    incrementAttempts(cache, attKey, lockKey);
    return jsonResponse({ success: false, error: 'Telefon veya şifre hatalı' });
  }

  const storedHash = String(row[COL_HASH - 1] || '').trim();
  const storedSalt = String(row[COL_SALT - 1] || '').trim();

  if (!storedHash) {
    return jsonResponse({ success: false, error: 'Şifre henüz belirlenmemiş. İlk girişi yapın.' });
  }

  if (hashPassword(storedSalt, password) !== storedHash) {
    incrementAttempts(cache, attKey, lockKey);
    return jsonResponse({ success: false, error: 'Telefon veya şifre hatalı' });
  }

  cache.remove(attKey);
  return jsonResponse({ success: true, token: createToken(phone) });
}

// ── me ─────────────────────────────────────────────────
function meHandler(e) {
  const payload = verifyToken(e.parameter.token || '');
  if (!payload) {
    return jsonResponse({ found: false, error: 'Oturum süresi doldu. Lütfen tekrar giriş yapın.' });
  }
  const members = getMembers(payload.p);
  return jsonResponse({ found: members.length > 0, members: members });
}

// ── Sheet yardımcıları ─────────────────────────────────
function getSheet() {
  return SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
}

function findRow(phone) {
  const rows = getSheet().getDataRange().getValues();
  for (let i = 1; i < rows.length; i++) {
    if (normalizePhone(String(rows[i][COL_TELEFON - 1])) === phone) return rows[i];
  }
  return null;
}

function getMembers(phone) {
  const rows   = getSheet().getDataRange().getValues();
  const result = [];
  for (let i = 1; i < rows.length; i++) {
    const r = rows[i];
    if (normalizePhone(String(r[COL_TELEFON - 1])) !== phone) continue;
    result.push({
      ad:        String(r[COL_AD        - 1] || ''),
      brans:     String(r[COL_BRANS     - 1] || ''),
      baslangic: fmtDate(r[COL_BASLANGIC - 1]),
      bitis:     fmtDate(r[COL_BITIS    - 1]),
      hafta:     String(r[COL_HAFTA     - 1] || ''),
      odeme:     String(r[COL_ODEME     - 1] || '')
    });
  }
  return result;
}

// ── Telefon normalize ──────────────────────────────────
function normalizePhone(raw) {
  let d = String(raw).replace(/\D/g, '');
  if (d.startsWith('90')) d = d.slice(2);
  if (d.startsWith('0'))  d = d.slice(1);
  return d;
}

// ── Tarih format ───────────────────────────────────────
function fmtDate(val) {
  if (!val) return '';
  if (val instanceof Date) {
    return String(val.getDate()).padStart(2, '0') + '.'
         + String(val.getMonth() + 1).padStart(2, '0') + '.'
         + val.getFullYear();
  }
  return String(val);
}

// ── Şifre hash ─────────────────────────────────────────
function generateSalt() {
  return Utilities.getUuid().replace(/-/g, '').slice(0, 16);
}

function hashPassword(salt, password) {
  const bytes = Utilities.computeDigest(
    Utilities.DigestAlgorithm.SHA_256,
    salt + password,
    Utilities.Charset.UTF_8
  );
  return bytes.map(b => ((b < 0 ? b + 256 : b).toString(16).padStart(2, '0'))).join('');
}

// ── Rate limiting ──────────────────────────────────────
function incrementAttempts(cache, attKey, lockKey) {
  const n = parseInt(cache.get(attKey) || '0') + 1;
  if (n >= MAX_ATTEMPTS) {
    cache.put(lockKey, '1', LOCK_TTL);
    cache.remove(attKey);
  } else {
    cache.put(attKey, String(n), LOCK_TTL);
  }
}

// ── HMAC Token ─────────────────────────────────────────
function createToken(phone) {
  const payloadStr = JSON.stringify({ p: phone, exp: Math.floor(Date.now() / 1000) + 2592000 });
  const payloadB64 = Utilities.base64EncodeWebSafe(payloadStr).replace(/=+$/, '');
  return payloadB64 + '.' + hmacSign(payloadB64);
}

function verifyToken(token) {
  if (!token) return null;
  const dot = token.lastIndexOf('.');
  if (dot === -1) return null;
  const payloadB64 = token.slice(0, dot);
  const sig        = token.slice(dot + 1);
  try {
    if (hmacSign(payloadB64) !== sig) return null;
    const payload = JSON.parse(
      Utilities.newBlob(Utilities.base64DecodeWebSafe(payloadB64)).getDataAsString()
    );
    if (payload.exp < Math.floor(Date.now() / 1000)) return null;
    return payload;
  } catch (err) {
    Logger.log('verifyToken: ' + err);
    return null;
  }
}

function hmacSign(data) {
  const secret = PropertiesService.getScriptProperties().getProperty('TOKEN_SECRET');
  if (!secret) throw new Error('TOKEN_SECRET tanımlı değil. Script Properties ayarla.');
  const sig = Utilities.computeHmacSha256Signature(data, secret, Utilities.Charset.UTF_8);
  return Utilities.base64EncodeWebSafe(sig).replace(/=+$/, '');
}

function jsonResponse(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}
```

- [ ] **Step 2: Apps Script editöründe güncelle**

1. Apps Script editörünü aç (Uzantılar > Apps Script veya script.google.com)
2. `Code.gs` içeriğini tamamen sil
3. Yukarıdaki kodu yapıştır → Ctrl+S ile kaydet

- [ ] **Step 3: Commit**

```bash
git add apps-script/Code.gs
git commit -m "feat: Apps Script auth backend v2 — checkStatus/setPassword/login/me"
```

---

## Task 4: Apps Script — Yeni Sürüm Deploy Et

**Files:**
- Modify: Apps Script deployment (manual)

- [ ] **Step 1: Sütun numaralarını doğrula**

Sheet'in gerçek sütun sırasına göre `Code.gs` üstündeki sabitleri güncelle ve kaydet.

- [ ] **Step 2: Yeni sürüm deploy et**

Apps Script editöründe:
1. Sağ üst → "Deploy" → "Manage deployments"
2. Mevcut Web App deployment'ı seç → kalem ikonu (Edit)
3. Version: **"New version"** seç
4. Execute as: "Me", Who has access: "Anyone" olduğunu doğrula
5. "Deploy" tıkla → yetki iste → Google hesabıyla onayla

URL değişmez. `PORTAL_URL` sabiti `uye-girisi.html`'de aynı kalır.

---

## Task 5: Frontend — CSS Ekle

**Files:**
- Modify: `uye-girisi.html`

- [ ] **Step 1: CSS bloğunu bul ve ekle**

`uye-girisi.html`'de şu satırı bul:
```css
    .kart-uyari-dolmus a { color: #DC2626; }
```

Bu satırdan hemen sonrasına şu CSS bloğunu ekle:

```css

    /* ══════════════════════════════
       AUTH FORM
    ══════════════════════════════ */
    .auth-field { margin-bottom: 16px; }
    .auth-field label {
      display: block;
      font-size: .82rem;
      font-weight: 600;
      color: var(--ink-m);
      text-transform: uppercase;
      letter-spacing: .5px;
      margin-bottom: 8px;
    }
    .auth-field input[type="password"],
    .auth-field input[type="text"] {
      width: 100%;
      padding: 13px 16px;
      border: 2px solid var(--line);
      border-radius: 10px;
      font-size: 1rem;
      font-family: 'Inter', sans-serif;
      color: var(--ink);
      background: var(--paper);
      outline: none;
      transition: border-color .2s;
    }
    .auth-field input:focus { border-color: var(--primary); background: #fff; }

    .auth-phone-badge {
      display: flex;
      align-items: center;
      gap: 8px;
      background: var(--paper-s);
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 10px 14px;
      font-size: .9rem;
      font-weight: 600;
      color: var(--ink-s);
      margin-bottom: 20px;
    }
    .auth-phone-badge::before { content: '📱 '; }

    .pw-strength {
      margin-top: 6px;
      font-size: .8rem;
      font-weight: 600;
      min-height: 18px;
    }
    .pw-strength.weak   { color: var(--danger); }
    .pw-strength.medium { color: #c05c00; }
    .pw-strength.strong { color: var(--success); }

    .kvkk-box {
      margin-bottom: 14px;
      border: 1px solid var(--line);
      border-radius: 10px;
      overflow: hidden;
    }
    .kvkk-scroll {
      height: 160px;
      overflow-y: auto;
      padding: 14px 16px;
      font-size: .78rem;
      line-height: 1.65;
      color: var(--ink-m);
      background: var(--paper);
      white-space: pre-wrap;
    }
    .kvkk-check-row {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      margin-bottom: 20px;
      cursor: pointer;
      font-size: .85rem;
      font-weight: 500;
      text-transform: none;
      letter-spacing: normal;
      color: var(--ink-s);
      line-height: 1.5;
    }
    .kvkk-check-row input[type="checkbox"] {
      flex-shrink: 0;
      margin-top: 2px;
      width: 16px;
      height: 16px;
      accent-color: var(--primary);
      cursor: pointer;
    }

    .btn-back {
      display: block;
      width: 100%;
      margin-top: 10px;
      background: none;
      border: none;
      font-size: .88rem;
      font-weight: 600;
      color: var(--ink-m);
      cursor: pointer;
      font-family: 'Inter', sans-serif;
      padding: 8px;
      transition: color .2s;
    }
    .btn-back:hover { color: var(--ink); }

    .btn-logout {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: none;
      border: 1.5px solid var(--line);
      border-radius: 10px;
      padding: 9px 16px;
      font-size: .88rem;
      font-weight: 600;
      color: var(--ink-m);
      cursor: pointer;
      font-family: 'Inter', sans-serif;
      margin-bottom: 20px;
      transition: color .2s, border-color .2s;
    }
    .btn-logout:hover { color: var(--danger); border-color: var(--danger); }
```

- [ ] **Step 2: Commit**

```bash
git add uye-girisi.html
git commit -m "feat: auth form CSS eklendi"
```

---

## Task 6: Frontend — HTML Güncelle

**Files:**
- Modify: `uye-girisi.html`

- [ ] **Step 1: Hero metnini güncelle**

Şunu bul:
```html
  <p>Telefon numaranızla üyelik bilgilerinize ulaşın</p>
```
Şununla değiştir:
```html
  <p>Telefon numaranız ve şifrenizle üyelik bilgilerinize güvenle ulaşın</p>
```

- [ ] **Step 2: search-card içeriğini değiştir**

Şu bloğu bul (tam eşleşme):
```html
  <div class="search-card">
    <label for="sorguInput">TELEFON NUMARASI</label>
    <div class="input-row">
      <input
        type="text"
        id="sorguInput"
        placeholder="Örn: 0532 123 45 67"
        autocomplete="off"
        onkeydown="if(event.key==='Enter') sorgula()"
      >
      <button class="btn-sorgula" id="btnSorgula" onclick="sorgula()">
        Sorgula
      </button>
    </div>
  </div>
```

Şununla değiştir:
```html
  <div class="search-card">

    <!-- STATE: phone -->
    <div id="statePhone">
      <label for="phoneInput">TELEFON NUMARASI</label>
      <div class="input-row">
        <input type="text" id="phoneInput" placeholder="Örn: 0532 123 45 67"
          autocomplete="tel" inputmode="numeric"
          onkeydown="if(event.key==='Enter') goProceed()">
        <button class="btn-sorgula" id="btnProceed" onclick="goProceed()">İlerle</button>
      </div>
    </div>

    <!-- STATE: setPassword -->
    <div id="stateSetPw" style="display:none">
      <div class="auth-phone-badge" id="setPwBadge"></div>
      <div class="auth-field">
        <label>YENİ ŞİFRE</label>
        <input type="password" id="setPwInput" placeholder="En az 8 karakter"
          autocomplete="new-password" oninput="checkStrength()"
          onkeydown="if(event.key==='Enter') document.getElementById('setPwConfirm').focus()">
        <div class="pw-strength" id="pwStrength"></div>
      </div>
      <div class="auth-field">
        <label>ŞİFRE TEKRAR</label>
        <input type="password" id="setPwConfirm" placeholder="Şifrenizi tekrar girin"
          autocomplete="new-password"
          onkeydown="if(event.key==='Enter') doSetPassword()">
      </div>
      <div class="kvkk-box">
        <div class="kvkk-scroll">CİMCİMPARK JİMNASTİK MERKEZİ KİŞİSEL VERİLERİN KORUNMASI AYDINLATMA METNİ

Bu aydınlatma metni, 6698 sayılı Kişisel Verilerin Korunması Kanunu'nun (KVKK) 10. maddesi uyarınca, veri sorumlusu sıfatıyla Cimcimpark Jimnastik Merkezi tarafından, üyelerimizin üyelik takip işlemlerinin dijital ortamda güvenli bir şekilde yürütülmesi amacıyla hazırlanmıştır.

1. İşlenen Kişisel Verileriniz ve İşlenme Amaçları:
Tarafımıza ait web sitesi üzerinden üyelik durumunuzu (üyelik başlangıç ve bitiş tarihleri) sorgulayabilmeniz amacıyla; adınız-soyadınız, telefon numaranız ve sistem güvenliği için kendi belirleyeceğiniz şifreniz işlenmektedir. Bu veriler, üyelik haklarınızın takibi ve yetkisiz üçüncü kişilerin verilerinize erişmesinin engellenmesi amaçlarıyla sınırlı olarak işlenir.

2. Kişisel Verilerin Aktarılması:
Sitede işlenen kişisel verileriniz, yasal yükümlülüklerin yerine getirilmesi amacıyla yetkili kamu kurum ve kuruluşları (talep halinde adli makamlar vb.) dışında hiçbir üçüncü şahıs, kurum veya şirketle kesinlikle paylaşılmamakta ve aktarılmamaktadır.

3. Veri Toplamanın Yöntemi ve Hukuki Sebebi:
Kişisel verileriniz, kulübümüze üye olurken verdiğiniz bilgilere istinaden, web sitemiz üzerindeki formlar aracılığıyla tamamen elektronik ortamda toplanmaktadır. Bu süreç, KVKK Madde 5/2-c (Bir sözleşmenin kurulması veya ifasıyla doğrudan doğruya ilgili olması) ve veri sorumlusunun hukuki yükümlülüğü kapsamında işlenmektedir.

4. Haklarınız:
KVKK'nın 11. maddesi uyarınca Cimcimpark'a başvurarak; verilerinizin işlenip işlenmediğini öğrenme, işlenmişse bilgi talep etme, verilerin silinmesini veya düzeltilmesini isteme haklarına sahipsiniz. Başvurularınızı merkezimize yazılı olarak veya kayıtlı iletişim kanallarımız üzerinden iletebilirsiniz.</div>
      </div>
      <label class="kvkk-check-row">
        <input type="checkbox" id="kvkkCheck">
        <span>Kişisel Verilerin Korunması Aydınlatma Metnini okudum ve onaylıyorum.</span>
      </label>
      <button class="btn-sorgula" id="btnSetPw" onclick="doSetPassword()">Kaydet ve Giriş Yap</button>
      <button class="btn-back" onclick="goBack()">← Geri</button>
    </div>

    <!-- STATE: login -->
    <div id="stateLogin" style="display:none">
      <div class="auth-phone-badge" id="loginBadge"></div>
      <div class="auth-field">
        <label>ŞİFRE</label>
        <input type="password" id="loginPwInput" placeholder="Şifrenizi girin"
          autocomplete="current-password"
          onkeydown="if(event.key==='Enter') doLogin()">
      </div>
      <button class="btn-sorgula" id="btnLogin" onclick="doLogin()">Giriş Yap</button>
      <button class="btn-back" onclick="goBack()">← Geri</button>
    </div>

    <!-- STATE: card (sadece logout butonu) -->
    <div id="stateCard" style="display:none">
      <button class="btn-logout" onclick="doLogout()">Çıkış Yap</button>
    </div>

  </div>
```

- [ ] **Step 3: Commit**

```bash
git add uye-girisi.html
git commit -m "feat: auth state HTML (phone/setPassword/login/card)"
```

---

## Task 7: Frontend — JavaScript Yeniden Yaz

**Files:**
- Modify: `uye-girisi.html` (`<script>` bloğu)

- [ ] **Step 1: Mevcut script bloğunu bul**

Dosyada `<script>` satırını bul (yaklaşık 685. satır). `<script>` ... `</script>` bloğunun tamamını sil ve şununla değiştir:

```html
<script>
  // ── Scroll shadow ──
  const hdr = document.getElementById('siteHeader');
  window.addEventListener('scroll', () => hdr.classList.toggle('scrolled', window.scrollY > 8), { passive: true });

  // ── Config ──
  const PORTAL_URL = 'https://script.google.com/macros/s/AKfycbww3x95xMK1Ccos44OxhAvQhML6MHlGvceU3jYP3oGzhDPQmyOLhACGqDQgcbsp7xkJRQ/exec';
  const TOKEN_KEY  = 'ccp_token';

  // ── State ──
  let currentPhone = '';

  // ── Sayfa yüklenince token kontrol ──
  (function init() {
    const token = localStorage.getItem(TOKEN_KEY);
    if (!token) return;
    document.getElementById('statePhone').style.display = 'none';
    setLoading(true);
    fetchMe(token);
  })();

  // ── STATE: phone ──────────────────────────────
  function goProceed() {
    const raw = document.getElementById('phoneInput').value.trim();
    const q   = normalizePhone(raw);
    if (q.length !== 10) { showMsg('Geçerli bir telefon numarası girin. Örn: 0532 123 45 67', false); return; }

    const btn = document.getElementById('btnProceed');
    btn.disabled = true; btn.textContent = '...';
    clearResults();

    apiFetch('checkStatus', { q })
      .then(data => {
        btn.disabled = false; btn.textContent = 'İlerle';
        if (data.error) { showMsg(data.error, true); return; }
        if (!data.exists) { showMsg('Bu telefon numarasına kayıtlı üyelik bulunamadı.', false); return; }
        currentPhone = q;
        if (data.hasPassword) {
          showState('stateLogin');
          document.getElementById('loginBadge').textContent = formatPhone(q);
          setTimeout(() => document.getElementById('loginPwInput').focus(), 100);
        } else {
          showState('stateSetPw');
          document.getElementById('setPwBadge').textContent = formatPhone(q);
          setTimeout(() => document.getElementById('setPwInput').focus(), 100);
        }
      })
      .catch(err => { btn.disabled = false; btn.textContent = 'İlerle'; showMsg('Bağlantı hatası: ' + err.message, true); });
  }

  // ── STATE: setPassword ────────────────────────
  function checkStrength() {
    const pw = document.getElementById('setPwInput').value;
    const el = document.getElementById('pwStrength');
    if (!pw)           { el.textContent = ''; el.className = 'pw-strength'; return; }
    if (pw.length < 8) { el.textContent = '✗ Çok kısa (en az 8 karakter)'; el.className = 'pw-strength weak'; return; }
    const score = [/\d/.test(pw), /[A-Z]/.test(pw), /[^A-Za-z0-9]/.test(pw)].filter(Boolean).length;
    if (score >= 2) { el.textContent = '✓ Güçlü şifre';                              el.className = 'pw-strength strong'; }
    else            { el.textContent = '⚠ Orta — rakam, büyük harf veya sembol ekle'; el.className = 'pw-strength medium'; }
  }

  function doSetPassword() {
    const pw      = document.getElementById('setPwInput').value;
    const confirm = document.getElementById('setPwConfirm').value;
    const kvkk    = document.getElementById('kvkkCheck').checked;

    if (pw.length < 8)  { showMsg('Şifre en az 8 karakter olmalı.', false); return; }
    if (pw !== confirm) { showMsg('Şifreler eşleşmiyor.', false); return; }
    if (!kvkk)          { showMsg('KVKK metnini okuyup onaylamanız gerekiyor.', false); return; }

    const btn = document.getElementById('btnSetPw');
    btn.disabled = true; btn.textContent = '...';

    apiFetch('setPassword', { q: currentPhone, password: pw, kvkk: 'true' })
      .then(data => {
        btn.disabled = false; btn.textContent = 'Kaydet ve Giriş Yap';
        if (!data.success) { showMsg(data.error || 'Bir hata oluştu.', true); return; }
        localStorage.setItem(TOKEN_KEY, data.token);
        fetchMe(data.token);
      })
      .catch(err => { btn.disabled = false; btn.textContent = 'Kaydet ve Giriş Yap'; showMsg('Bağlantı hatası: ' + err.message, true); });
  }

  // ── STATE: login ──────────────────────────────
  function doLogin() {
    const pw = document.getElementById('loginPwInput').value;
    if (!pw) { showMsg('Şifrenizi girin.', false); return; }

    const btn = document.getElementById('btnLogin');
    btn.disabled = true; btn.textContent = '...';
    clearResults();

    apiFetch('login', { q: currentPhone, password: pw })
      .then(data => {
        btn.disabled = false; btn.textContent = 'Giriş Yap';
        if (!data.success) { showMsg(data.error || 'Telefon veya şifre hatalı.', true); return; }
        localStorage.setItem(TOKEN_KEY, data.token);
        fetchMe(data.token);
      })
      .catch(err => { btn.disabled = false; btn.textContent = 'Giriş Yap'; showMsg('Bağlantı hatası: ' + err.message, true); });
  }

  // ── STATE: card ───────────────────────────────
  function fetchMe(token) {
    setLoading(true);
    clearResults();
    apiFetch('me', { token })
      .then(data => {
        setLoading(false);
        if (!data.found) {
          localStorage.removeItem(TOKEN_KEY);
          showState('statePhone');
          showMsg(data.error || 'Üyelik bilgisi bulunamadı.', false);
          return;
        }
        showState('stateCard');
        data.members.forEach(renderCard);
      })
      .catch(err => { setLoading(false); showMsg('Bağlantı hatası: ' + err.message, true); });
  }

  function doLogout() {
    localStorage.removeItem(TOKEN_KEY);
    showState('statePhone');
    document.getElementById('phoneInput').value = '';
    clearResults();
    currentPhone = '';
  }

  function goBack() {
    showState('statePhone');
    clearResults();
    document.getElementById('loginPwInput').value     = '';
    document.getElementById('setPwInput').value       = '';
    document.getElementById('setPwConfirm').value     = '';
    document.getElementById('kvkkCheck').checked      = false;
    document.getElementById('pwStrength').textContent = '';
    document.getElementById('pwStrength').className   = 'pw-strength';
  }

  // ── State machine ─────────────────────────────
  const ALL_STATES = ['statePhone', 'stateSetPw', 'stateLogin', 'stateCard'];
  function showState(id) {
    ALL_STATES.forEach(s => document.getElementById(s).style.display = s === id ? 'block' : 'none');
    clearResults();
  }

  // ── Kart render ───────────────────────────────
  function renderCard(m) {
    const bitisDate    = parseDate(m.bitis);
    const bugun        = new Date(); bugun.setHours(0, 0, 0, 0);
    const suresiDolmus = bitisDate && bitisDate < bugun;

    const uyari = suresiDolmus
      ? '<div class="kart-uyari kart-uyari-dolmus"><span>❌</span>'
        + '<span>Üyeliğinizin süresi dolmuştur. Yeniden başlamak için bizi arayın: '
        + '<a href="tel:+905392437606">0539 243 76 06</a></span></div>'
      : m.hafta === 'Son Hafta'
        ? '<div class="kart-uyari kart-uyari-sonhafta"><span>⚠️</span>'
          + '<span>Üyeliğiniz bu hafta sona eriyor. Yenilemek için bizi arayın: '
          + '<a href="tel:+905392437606">0539 243 76 06</a></span></div>'
        : '';

    document.getElementById('cards').insertAdjacentHTML('beforeend',
      '<div class="member-card">'
      + '<div class="card-header">'
      +   '<div class="avatar">👤</div>'
      +   '<div><div class="card-name">' + esc(m.ad) + '</div>'
      +   '<div class="card-brans">🥋 ' + esc(m.brans) + '</div></div>'
      + '</div>'
      + '<div class="card-rows">'
      +   row('📅', 'Başlangıç', esc(m.baslangic))
      +   row('📅', 'Bitiş',     esc(m.bitis))
      +   '<div class="card-row"><span class="icon">📍</span><span class="lbl">Dönem</span>'
      +     '<span class="badge badge-hafta">📍 ' + esc(m.hafta) + '</span></div>'
      +   '<div class="card-row"><span class="icon">💳</span><span class="lbl">Ödeme</span>' + odemeBadge(m.odeme) + '</div>'
      + '</div>'
      + uyari
      + '</div>'
    );
  }

  function row(icon, lbl, val) {
    return '<div class="card-row"><span class="icon">' + icon + '</span>'
      + '<span class="lbl">' + lbl + '</span><span class="val">' + val + '</span></div>';
  }

  function odemeBadge(o) {
    if (o === 'Ödendi')      return '<span class="badge badge-odendi">✓ Ödendi</span>';
    if (o === 'Kısmi Ödeme') return '<span class="badge badge-kismi">◑ Kısmi Ödeme</span>';
    return '<span class="badge badge-bekliyor">! Bekliyor</span>';
  }

  function parseDate(str) {
    if (!str) return null;
    const sep   = str.includes('.') ? '.' : '-';
    const parts = str.split(sep);
    if (parts.length !== 3) return null;
    const d = str.includes('.')
      ? new Date(parts[2], parts[1] - 1, parts[0])
      : new Date(parts[0], parts[1] - 1, parts[2]);
    return isNaN(d.getTime()) ? null : d;
  }

  // ── Yardımcılar ───────────────────────────────
  function normalizePhone(raw) {
    let d = raw.replace(/\D/g, '');
    if (d.startsWith('90')) d = d.slice(2);
    if (d.startsWith('0'))  d = d.slice(1);
    return d;
  }

  function formatPhone(p10) {
    return '0' + p10.slice(0,3) + ' ' + p10.slice(3,6) + ' ' + p10.slice(6,8) + ' ' + p10.slice(8);
  }

  function apiFetch(action, params) {
    const qs = Object.entries(params)
      .map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(v)).join('&');
    return fetch(PORTAL_URL + '?action=' + action + '&' + qs)
      .then(r => { if (!r.ok) throw new Error('HTTP ' + r.status); return r.json(); });
  }

  function setLoading(on) {
    document.getElementById('spinner').style.display = on ? 'block' : 'none';
  }

  function clearResults() {
    document.getElementById('cards').innerHTML      = '';
    document.getElementById('msgBox').style.display = 'none';
  }

  function showMsg(txt, isError) {
    const el = document.getElementById('msgBox');
    el.textContent   = txt;
    el.className     = 'msg-box' + (isError ? ' error' : '');
    el.style.display = 'block';
  }

  function esc(str) {
    return String(str)
      .replace(/&/g,'&amp;').replace(/</g,'&lt;')
      .replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }
</script>
```

- [ ] **Step 2: Commit**

```bash
git add uye-girisi.html
git commit -m "feat: auth state machine JS — phone/setPassword/login/card/logout"
```

---

## Task 8: Manuel Test ve Final Commit

**Files:**
- Test: browser

- [ ] **Step 1: Sunucuyu başlat**

```bash
python -m http.server 3000
```

`http://localhost:3000/uye-girisi.html` aç.

- [ ] **Step 2: İlk giriş akışı (şifresiz üye)**

1. Var olan bir üyenin telefon numarasını gir → "İlerle"
2. **Beklenen:** [ŞİFRE BELİRLE] ekranı açılır, telefon numarası badge'de görünür
3. KVKK kutusu kaydırılabilir olmalı
4. Checkbox işaretlenmeden "Kaydet" → `"KVKK metnini okuyup onaylamanız gerekiyor."` mesajı
5. 6 karakterli şifre + KVKK onay → `"Şifre en az 8 karakter olmalı."` mesajı
6. Farklı şifre + tekrar → `"Şifreler eşleşmiyor."` mesajı
7. Doğru doldur → üyelik kartı görünür, `localStorage['ccp_token']` dolu

- [ ] **Step 3: Token persistence testi**

1. Sayfayı yenile → **doğrudan** üyelik kartı görünmeli (login formu atlanmalı)
2. "Çıkış Yap" → phone state'e dön, localStorage temizlenmeli

- [ ] **Step 4: Tekrar giriş akışı (şifreli üye)**

1. Telefon gir → "İlerle" → **Beklenen:** [ŞİFRE GİR] ekranı
2. Yanlış şifre → `"Telefon veya şifre hatalı"` mesajı
3. Doğru şifre → üyelik kartı

- [ ] **Step 5: Brute-force testi**

1. Yanlış şifreyi 5 kez gir → `"Çok fazla yanlış deneme. 15 dakika sonra tekrar deneyin."` mesajı

- [ ] **Step 6: Güvenlik kontrolü**

1. Başka birinin telefon numarasını gir → şifre belirle / şifre gir ekranı çıkar ama kendi şifresi olmadan diğerinin bilgisine **ulaşamaz** ✓

- [ ] **Step 7: Final commit**

```bash
git add .
git commit -m "feat: üye giriş paneli tamamlandı — telefon+şifre auth, KVKK, brute-force koruması"
```
